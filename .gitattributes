# Normalize line endings for OS-agnostic development (Windows, WSL, Linux CI).
* text=auto eol=lf

# Example env files must stay LF for bash and python-dotenv.
.env.example text eol=lf

# Windows-only scripts keep CRLF when present.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
